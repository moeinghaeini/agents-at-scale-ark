apiVersion: ark.mckinsey.com/v1alpha1
kind: Agent
metadata:
  name: coordinator
spec:
  modelRef:
    name: test-model
  prompt: "You are the coordinator. If there is a final report already, just repeat it."

