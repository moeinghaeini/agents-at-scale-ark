apiVersion: ark.mckinsey.com/v1alpha1
kind: Model
metadata:
  name: default
spec:
  provider: anthropic
  model:
    value: claude-sonnet-4-20250514
  config:
    anthropic:
      baseUrl:
        value: ($provider.url)
      apiKey:
        valueFrom:
          secretKeyRef:
            name: test-model-token
            key: token
