apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: engineering-build-test-
  namespace: default
spec:
  workflowTemplateRef:
    name: engineering-build-test
  serviceAccountName: argo-workflow
